# Language setting
language: python

# Version number
python:
  - 2.7

build:
  ci:
    - python --version
    - cat /tmp/ssh/testsshKey

integrations:
  generic:
    - integrationName: test-sshKey
      type: sshKey
