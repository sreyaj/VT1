# Language setting
language: python

# Version number
python:
  - 2.7

build:
  ci:
    - python --version
    - cat /tmp/ssh/oldssh1

integrations:
  key:
    - integrationName: old-ssh-1
      type: ssh-key
