# Language setting
language: python

# Version number
python:
  - 2.7

build:
  ci:
    - python --version
    - cd deep_gh_ssh/
    - ls -a

integrations:
  generic:
    - integrationName: deep_gh_ssh
      type: sshKey
