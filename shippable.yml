# Language setting
language: python

# Version number
python:
  - 2.7

build:
  ci:
    - python --version
    - shipctl put_resource_state "test_params" TRIGGER_REASON BaseImgChanged
    #- sleep 1m

integrations:
  notifications:
    - integrationName: test-old-webhook   #issue will get created in github samleNod project
      type: webhook
      payload:
        - title=Failure - $REPO_FULL_NAME - $BUILD_NUMBER
        - body=Shippable Run $BUILD_NUMBER ($BUILD_URL) failed for $COMPARE_URL 
      on_start: always
      on_success: always
      on_failure: always   
 

    - integrationName: test-generictrigger #samplNod project in shiptest-rc-ow will trigger
      type: webhook
      payload:
        - BUILD_NUM=$SHIPPABLE_JOB_NUMBER 
        - BUILD_NUM=$SHIPPABLE_BUILD_NUMBER
        - FOO=FUBU
        - GOO=GUBU
   #   on_start: always
      on_success: always
    #  on_failure: always
