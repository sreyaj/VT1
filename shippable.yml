# Language setting
language: python

# Version number
python:
  - 2.7

build:
  ci:
    - python --version
  #  - cat /tmp/ssh/old-ssh-1

integrations:
  key:
    - integrationName: old-ssh-1
      type: sshKey
