# Language setting
language: python

# Version number
python:
  - 2.7

build:
  ci:
    - python --version
    - cat /tmp/ssh/deep_gh_ssh

integrations:
  generic:
    - integrationName: deep_gh_ssh
      type: sshKey
