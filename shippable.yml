# Language setting
language: python

# Version number
python:
  - 2.7

build:
  ci:
    - python --version
    - cat /tmp/ssh/sshkey

integrations:
  key:
    - integrationName: test-sshKey
      type: sshKey
