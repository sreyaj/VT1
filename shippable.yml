# Language setting
language: python

# Version number
python:
  - 2.7

build:
  ci:
    - python --version
    - cat /tmp/ssh/testssh

integrations:
  key:
    - integrationName: testssh
      type: sshKey
